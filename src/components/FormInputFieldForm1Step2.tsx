// This is a skeleton starter React component generated by Plasmic.
// This file is owned by you, feel free to edit as you see fit.
import * as React from "react";
import {
  PlasmicFormInputFieldForm1Step2,
  DefaultFormInputFieldForm1Step2Props
} from "./plasmic/netir_fr/PlasmicFormInputFieldForm1Step2";

// Your component props start with props for variants and slots you defined
// in Plasmic, but you can add more here, like event handlers that you can
// attach to named nodes in your component.
//
// If you don't want to expose certain variants or slots as a prop, you can use
// Omit to hide them:
//
// interface FormInputFieldForm1Step2Props extends Omit<DefaultFormInputFieldForm1Step2Props, "hideProps1"|"hideProp2"> {
//   // etc.
// }
//
// You can also stop extending from DefaultFormInputFieldForm1Step2Props altogether and have
// total control over the props for your component.
export interface FormInputFieldForm1Step2Props
  extends DefaultFormInputFieldForm1Step2Props {}

function FormInputFieldForm1Step2(props: FormInputFieldForm1Step2Props) {
  // Use PlasmicFormInputFieldForm1Step2 to render this component as it was
  // designed in Plasmic, by activating the appropriate variants,
  // attaching the appropriate event handlers, etc.  You
  // can also install whatever React hooks you need here to manage state or
  // fetch data.
  //
  // Props you can pass into PlasmicFormInputFieldForm1Step2 are:
  // 1. Variants you want to activate,
  // 2. Contents for slots you want to fill,
  // 3. Overrides for any named node in the component to attach behavior and data,
  // 4. Props to set on the root node.
  //
  // By default, we are just piping all FormInputFieldForm1Step2Props here, but feel free
  // to do whatever works for you.

  return <PlasmicFormInputFieldForm1Step2 {...props} />;
}

export default FormInputFieldForm1Step2;
